/**
 * Default number of days for time range filtering
 */
export const DEFAULT_DAYS = 30;

/**
 * Domain for email addresses (e.g., "sentry.io")
 */
export const DOMAIN = process.env.NEXT_PUBLIC_DOMAIN || '';
